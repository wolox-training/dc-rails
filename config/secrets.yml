# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.
development:
  secret_key_base: d99362ecd583635695f417a833e53615bba168337907ccf320b831e0613a9743c20483bdfa902e77405def172d666a23dd3a02c5ec8087538a91532cf4ffea23
  devise_mail_sender: david.carrillo@wolox.com.ar
  mailer_domain: <%= ENV["MAILER_DOMAIN"] %>

test:
  secret_key_base: 8537ad237b7deb3b4475f1f3791615f1810d05033e3d5bf029069c0a47113f88f4299c5616bebded40011619e627f2d14e52e480fb689308c5591f1a8cb93145
  devise_mail_sender: david.carrillo@wolox.com.ar
  mailer_domain: <%= ENV["MAILER_DOMAIN"] %>
# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  devise_mail_sender: <%= ENV["DEVISE_MAIL_SENDER"] %>
  mailer_domain: <%= ENV["MAILER_DOMAIN"] %>
